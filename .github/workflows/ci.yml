name: Build

on:
  push:
    branches: [main]
    paths-ignore: ['**/*.md']
  pull_request:
    branches: [main]
    paths-ignore: ['**/*.md']
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Restore
        run: dotnet restore Jellyfin.Plugin.Serializd.sln

      - name: Build
        run: dotnet build Jellyfin.Plugin.Serializd.sln -c Release --no-restore

      - name: Upload DLL artifact
        uses: actions/upload-artifact@v4
        with:
          name: Jellyfin.Plugin.Serializd
          path: Jellyfin.Plugin.Serializd/bin/Release/net9.0/Jellyfin.Plugin.Serializd.dll
